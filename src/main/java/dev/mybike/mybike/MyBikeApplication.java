package dev.mybike.mybike;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The main application class for the MyBikeApplication.
 * This is a Spring Boot application and serves as the entry point of the
 * application.
 * It initializes and runs the Spring Boot framework.
 */
@SpringBootApplication
public class MyBikeApplication {

    public static void main(String[] args) {
        SpringApplication.run(MyBikeApplication.class, args);
    }

}
